<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  
  <mapper namespace="com.iu.s5.member.MemberDAO" >
  	<select id="memberList" resultType="com.iu.s5.member.MemberVO">
  <!-- 			select * from 
			(select N.*, rownum R from 
			(select * from member where 
			
			<choose>
				<when test="kind == 'mi'">
					ID
				</when>
				<when test="kind == 'mn'">
					NAME
				</when>
				<when test="kind == 'mp'">
					PHONE
				</when>
				<otherwise>
					EMAIL
				</otherwise>
			</choose>
			
			
			
			like '%'||#{search}||'%' and num>=1 order by num desc) N)
			where R between #{startRow} and #{lastRow} -->
			
			select * from member
  	
  	</select>
  
  	<insert id="memberWrite" parameterType="com.iu.s5.member.MemberVO">
  		insert into member values(#{id},#{pw},#{name},#{email},#{phone},#{age})
  	</insert>
  
  
  
  </mapper>